# Gradle build configuration

# Enable native access to suppress JVM warnings about restricted methods
# Note: Some warnings may still appear during Gradle launcher initialization
org.gradle.jvmargs=-Xmx512m --enable-native-access=ALL-UNNAMED

# Enable configuration cache for faster builds
org.gradle.configuration-cache=true

# Quiet mode - reduce output verbosity
org.gradle.logging.level=lifecycle

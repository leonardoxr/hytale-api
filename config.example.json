{
  "enabled": true,
  "port": 8080,
  "bindAddress": "0.0.0.0",
  "tls": {
    "enabled": false,
    "certPath": "cert.pem",
    "keyPath": "key.pem",
    "keyPassword": null
  },
  "jwt": {
    "issuer": "hytale-api",
    "audience": "hytale-server",
    "tokenValiditySeconds": 3600,
    "refreshTokenValiditySeconds": 86400,
    "rsaKeyPath": "jwt-keypair.pem",
    "algorithm": "RS256"
  },
  "clients": [
    {
      "id": "admin",
      "secret": "$2a$12$LQv3c1yqBWVHxkd0LHAkCOYz6TtxMQJqhN8/X4L0Qd1U4M9FWML5m",
      "description": "Default admin client (password: admin)",
      "permissions": ["api.*"],
      "enabled": true
    },
    {
      "id": "monitor",
      "secret": "$2a$12$examplehash",
      "description": "Read-only monitoring client",
      "permissions": [
        "api.status.read",
        "api.players.read",
        "api.worlds.read",
        "api.websocket.connect",
        "api.websocket.subscribe.status"
      ],
      "enabled": true
    }
  ],
  "rateLimits": {
    "defaultRequestsPerMinute": 60,
    "burstSize": 10,
    "endpoints": {
      "/auth/token": {
        "requestsPerMinute": 10,
        "burstSize": 3
      },
      "/admin/*": {
        "requestsPerMinute": 30,
        "burstSize": 5
      }
    }
  },
  "cors": {
    "allowedOrigins": ["*"],
    "allowedMethods": ["GET", "POST", "PUT", "DELETE", "OPTIONS"],
    "allowedHeaders": ["Authorization", "Content-Type", "X-Request-ID"],
    "exposedHeaders": ["X-Request-ID", "X-RateLimit-Remaining"],
    "allowCredentials": true,
    "maxAge": 86400
  },
  "websocket": {
    "enabled": true,
    "path": "/ws",
    "maxConnections": 100,
    "pingIntervalSeconds": 30,
    "statusBroadcastIntervalSeconds": 5
  },
  "audit": {
    "enabled": true,
    "logRequests": true,
    "logResponses": false,
    "logAdminActions": true,
    "sensitiveFields": ["secret", "password", "token", "authorization"]
  }
}
